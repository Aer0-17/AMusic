import 'react-native-gesture-handler';  // 引入手势处理
import React from 'react';  // 引入 React
import { ExpoRoot } from 'expo-router';  // 引入 ExpoRouter

// App 组件
export default function App() {
  return <ExpoRoot />;
}
